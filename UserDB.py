import sqlite3

class Data:
    
    def __init__(self):
        
        self.head = None
        self.current_node = None
        self.previous_node = None

        self.username = pyscript.js(api)
        self.password = pyscript.js(api)
        self.dict = {}
        self.auth = False
    
    def firstNode(self):
        if self.login > "":
            self.head = int(1)
 
            nextNode()
             
    def nextNode(self):
         self.login = input(login_popup.pyscript)
 
         if self.login in maindb:
             return current_id
 
        
        
# if self.login in sqlite3(db.sql)
# once auth reutrn current id
# id stores class variables lifetime 
# if id is in db, call variables from relational db where id is foreign key
 
# if no id , create id current node + 1 and run previous script
# use previous node to credit current node, current_node = previous_node+1
# for first id in db, create self.head = 1
# id is always stores primary key in auth(db)
# id is foerign key in table store class var such as credit                
    
